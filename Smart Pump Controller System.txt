#define BLYNK_TEMPLATE_ID "TMPL3lpnLoVXC"
#define BLYNK_TEMPLATE_NAME "the baap company "
#define BLYNK_AUTH_TOKEN "KCxt9Qd5sBJ9_zRsQWyJdiKsrEJYBizR"
 
#include <ESP8266WiFi.h>
#include <BlynkSimpleEsp8266.h>
 

char ssid[] = "Redmi";
char pass[] = "12345789h";
 

#define RELAY_PIN D1
 

BLYNK_WRITE(V0) {
  int value = param.asInt(); digitalWrite(RELAY_PIN, value);
}
 
void setup() {
  Serial.begin(115200);
  pinMode(RELAY_PIN, OUTPUT);
  digitalWrite(RELAY_PIN, LOW); 
  Blynk.begin(BLYNK_AUTH_TOKEN, ssid, pass);
}
 
void loop() {
  Blynk.run();
}